# Archivos del sistema
.DS_Store
Thumbs.db
desktop.ini
*.log

# NO ignoramos downloads/app.apk ni downloads/documento.pdf:
# esos SÍ deben subirse al repositorio para que Netlify los publique.

# El APK que se publica vive en downloads/app.apk. La copia suelta en la raíz
# es solo para trabajo local y no se versiona.
/pdfsv.apk
