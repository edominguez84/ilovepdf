# Archivos del sistema
.DS_Store
Thumbs.db
desktop.ini
*.log

# NO ignoramos downloads/app.apk ni downloads/documento.pdf:
# esos SÍ deben subirse al repositorio para que Netlify los publique.

# Los archivos que se publican viven en downloads/. Las copias sueltas en la
# raíz son solo para trabajo local y no se versionan.
/pdfsv.apk
/siman_calendario.pdf
